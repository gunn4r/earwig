Added `earwig update`, which detects how earwig was installed (uv tool, pipx, or pip) and runs the matching upgrade; a source checkout is told to `git pull`. Added `earwig --version` (and an `earwig version` subcommand) to print the installed version.
