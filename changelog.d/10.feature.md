Added a `manual` namer (`--namer manual`) that prompts you to type each speaker's name interactively, with no LLM or inference required. Names remain available via `--namer claude` / `--namer local`.
