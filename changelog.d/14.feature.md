Added a `--device {cpu,cuda}` flag to pick the compute device for transcription and diarization (default `cpu`; `cuda` uses `float16` on an NVIDIA GPU). There is no `mps` option — the faster-whisper backend has no Apple-GPU support, so Apple Silicon transcribes on CPU.
