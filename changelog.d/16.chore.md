Pin the tested dependency ranges: `whisperx>=3.8,<3.9` and `pyannote-audio>=4,<5`, since whisperX's diarization API and default model drift across minor versions. `yt-dlp` is intentionally left uncapped so it keeps pace with YouTube changes.
